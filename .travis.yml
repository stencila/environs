language: nix

cache:
  directories:
    - /nix/store
  timeout: 600

services:
  - docker

install:
  - make setup

script:
  # Currently, adding these causes the build to exceed time limit (50mins)
  #- make images/base/node/
  - make images/base/py/
  - make images/base/r/
  #- make images/base/
  - make test
